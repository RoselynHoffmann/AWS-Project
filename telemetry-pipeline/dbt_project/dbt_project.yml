name: 'telemetry_pipeline'
version: '1.0.0'
config-version: 2
profile: 'telemetry_pipeline'
model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
models:
  telemetry_pipeline:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
